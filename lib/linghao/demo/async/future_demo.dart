import 'package:flutter/material.dart';

class FutureDemo extends StatelessWidget {
  const FutureDemo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Future Demo'),
      ),
      body: const Center(
        child: Text('Future Demo Page'),
      ),
    );
  }
}
